<div class="container">
    <div class="row">
        <div class="col-md-4 col-md-offset-4">
            {{error-alert hasError=hasError header=errorHeader message=errorMessage errorList=errorList}}

            <form {{action 'authenticate' on='submit'}}>
                <div class="form-group">
                    <label for="identification">Email address</label>
                    {{input type="email" class="form-control" placeholder="E-mail" id="identification" value=identification}}
                </div>
                <div class="form-group">
                    <label for="password">Password</label>
                    {{input type="password" class="form-control" placeholder="Password" value=password}}
                </div>
                <button type="submit" class="btn btn-primary">Login</button>
            </form>
        </div>
    </div>
</div>
