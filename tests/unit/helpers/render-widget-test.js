import {
  renderWidget
} from 'server-dash-client/helpers/render-widget';

module('RenderWidgetHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = renderWidget(42);
  ok(result);
});
